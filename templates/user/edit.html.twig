{% extends 'template.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    {% block pageTitle %}

    {% endblock %}
    <h1>Edit User</h1>

{#    {{ include('user/update_form.html.twig', {'button_label': 'Update'}) }}#}
{#    <a href="{{ path('app_dashboard') }}" class="btn btn-outline-primary me-3">{{ button_label|default('Back to list') }}</a>#}
{#    {{ include('user/_delete_form.html.twig') }}#}


{#    <div class="btn-group" role="group" aria-label="User actions">#}
        {{ include('user/update_form.html.twig', {'button_label': 'Update'}) }}
        <a href="{{ path('app_dashboard') }}" class="btn btn-outline-primary">{{ button_label|default('Back to list') }}</a>
{#        {{ include('user/_delete_form.html.twig') }}#}
{#    </div>#}
{% endblock %}
{#    <a href="{{ path('app_dashboard') }}">back to list</a>#}
